deploy:sendSlackNotice:
  image: jpmschuler/docker-curl:1.0.2
  stage: deploy
  variables:
    GIT_STRATEGY: none
  cache: {}
  rules:
    - !reference [ .rules_onlyversiontagandslack, rules ]
  script:
    - .gitlab/slackNoticeNewTag.sh
